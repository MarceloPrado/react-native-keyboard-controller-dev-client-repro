import { StatusBar } from "expo-status-bar";
import { StyleSheet, Text, View } from "react-native";
import { default as ExampleApp } from "./src/App";

export default function App() {
  return <ExampleApp />;
}
